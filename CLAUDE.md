@AGENTS.md

## Claude Code

- `AGENTS.md` is the canonical Agent Ember instruction source.
- This file exists only so Claude Code loads the same bootstrap instructions.
- Do not duplicate Agent Ember lifecycle rules here; update `AGENTS.md` instead.
