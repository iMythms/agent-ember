# 2026-06-17

## Session

- Ignition read `IDENTITY.md`, `KNOWLEDGE.md`; no prior notes existed.
- Created branch `codex/obsidian-second-brain` from `main`.
- Established Ember identity from current repository purpose and operator context.
- Added initial Obsidian second-brain architecture state.

## Decisions

- Repository memory remains the operational source of truth.
- Obsidian should be a one-way synthesis/index layer for version 1.
- Raw repo notes should not be mirrored wholesale; summarize into useful daily entries.

## Follow-ups

- Confirm Obsidian vault path.
- Decide implementation runtime for `ember sync`.
- Define schemas for generated Obsidian notes.

## Second Session

- Added `.ember/config.json` with null `vaultPath` until the actual Obsidian vault path is confirmed.
- Added Obsidian templates for project, agent, daily, decision, and pattern notes.
- Added `knowledge/obsidian-schemas.md` to define frontmatter and generated-block contracts.
- Added `knowledge/sync-protocol.md` to define dry-run, write, conflict, and privacy rules.
- Added `scripts/ember-sync.mjs` as the first executable dry-run planner; write mode is blocked until deterministic rendering is implemented.

## Updated Follow-ups

- Implement rendering for generated project, agent, and daily notes.
- Choose script/runtime shape after checking whether this repository should become a package or stay protocol-first.
- Set `vaultPath` only after verifying the local Obsidian vault location.
